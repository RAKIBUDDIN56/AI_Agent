Q: Can TableRF load millions of records?

Yes — using LazyLoad + server-side pagination.

Q: How do I add custom buttons in each row?

Use <ActionItem>.

Q: How do I show only filtered data?

Use the Filter="c => c.Age == 20" property.

Q: Can TableRF work with a list instead of DB?

Yes — use DataList.