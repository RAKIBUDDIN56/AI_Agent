Application Style

This section defines visual theme and style:

AppTheme sets the UI theme. The provided example uses "Classic".

AppStyle allows customization of fonts, colors, spacing, and layout.

RapidFireUI provides theming extensions via AppStyleRF, ensuring UI elements maintain consistency across the application.