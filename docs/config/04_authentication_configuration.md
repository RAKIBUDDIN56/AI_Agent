Authentication Configuration

Authentication settings include:

LoginType indicates the login mechanism. "LoginDB" enables database-based login.

JwtKeys.SecretKey stores a secret used to sign JWT tokens.

LoginFactory.Add allows registering custom login strategies such as StudentLoginFactory.

LoginContext defines authentication logic.

These settings enable secure access management for user accounts.