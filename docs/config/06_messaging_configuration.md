Messaging

Messaging services supported:

Firebase Cloud Messaging (FCM)

ConfigJsonPath refers to the Firebase configuration file.

Used to send push notifications.

Firebase Chat (FChat)

BasePath points to the real-time database endpoint.

AuthSecret controls authentication.

SMS & Voice Messaging

RequestUri and SecretKey define endpoints and authentication keys.

Email

Username and Password for sending emails using SMTP.